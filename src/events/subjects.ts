// List of channels/event names

// An enum is used as a type and it limits the valid options to the properties described in the enum.
export enum Subjects {
	TicketCreated = 'ticket:created',
	OrderUpdated = 'order:updated',
}
